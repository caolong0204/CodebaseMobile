import Config from 'react-native-config';

export const AppConfig = {
  APP_ENV: Config.APP_ENV,
  API_URL: Config.API_URL,
  APP_DISPLAY_NAME: Config.APP_DISPLAY_NAME,
  APP_DISPLAY_TEST: 'TEST',
};
